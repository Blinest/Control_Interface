# SoftUI 需求规格说明书（SRS）

**项目名称：** SoftUI  
**项目定位：** 绳驱两段式连续体机器人桌面控制、监测与实验数据平台  
**重构技术路线：** Tauri 2 + Rust + React + TypeScript + Three.js  
**文档版本：** v2.0-draft  
**文档日期：** 2026-07-14  
**需求基线：** `Blinest/Control_Interface` 现有 PyQt5 实现及《SoftUI 项目需求与技术说明书》  
**适用对象：** 产品负责人、机器人控制工程师、嵌入式工程师、前端/后端开发者、测试人员、AI 编码代理

---

## 1. 文档目的

本文档定义 SoftUI 重构版本的业务目标、产品范围、功能需求、非功能需求、数据要求、验收标准、迁移边界与交付物。

本文档回答“系统需要做什么、做到什么程度”，不规定具体代码实现。技术实现以配套的《SoftUI 开发技术设计说明书（TDS）》为准。

---

## 2. 项目背景

现有 SoftUI 使用 PyQt5 开发，已经具备以下能力：

1. 本地账号登录、注册、修改密码与角色区分；
2. 串口自动发现、手动连接和断线提示；
3. 多串口设备标签页；
4. 设备状态帧接收、拼帧、校验、解析与滤波；
5. 电机位置、速度、加速度控制；
6. 压力传感器校准；
7. 两段式连续体机器人弯曲角度和方向控制；
8. PID、压力阈值与电机反馈相关闭环控制；
9. 主动控制、循环寿命检测与命令超时重发；
10. 电机、传感器和弯曲角实时曲线；
11. 运行日志、原始十六进制日志、操作历史和 CSV 导出；
12. 本地 HTML 形式的 3D 查看功能；
13. 调试模式和虚拟设备入口。

现有实现可以支撑基础联调，但业务逻辑、串口生命周期、协议解析、控制策略、数据缓存和 UI 代码耦合较重；长期运行、自动化测试、打包交付、3D 联动和安全存储能力仍有改进空间。

本次重构不改变 SoftUI 的核心业务对象和既有硬件协议，而是将其升级为模块化、可测试、可扩展、可离线交付的现代桌面应用。

---

## 3. 建设目标

### 3.1 总体目标

建设一套面向绳驱连续体机器人的离线桌面上位机，满足以下目标：

1. 使用 Tauri 2 作为桌面容器，Rust 负责硬件通信、协议、控制、存储和日志；
2. 使用 React + TypeScript 构建可维护的工作区界面；
3. 使用 Three.js 加载并驱动连续体机器人三维模型；
4. 保持对现有串口协议和主要控制流程的兼容；
5. 支持多设备、实时监测、控制、曲线、会话、日志和诊断；
6. 所有核心能力均可在无真实硬件时通过模拟器开发和测试；
7. 以 Windows 为首要交付平台，并保留 Linux 支持；
8. 最终用户无需安装 Python、Node.js 或 Rust 开发环境。

### 3.2 重构成功标准

重构版本至少应满足：

- 覆盖现有 SoftUI 的全部高优先级功能；
- 关键控制命令与现有设备协议兼容；
- 串口 IO、数据记录和控制循环不阻塞界面线程；
- 具备可重复的协议、控制和数据导出自动化测试；
- 可在全新 Windows 机器安装运行；
- 可导入现有 SoftUI 的历史 CSV、用户数据或配置；
- 可在模拟器模式下完成主要功能演示。

---

## 4. 范围定义

### 4.1 本期范围（In Scope）

1. Windows 10/11 x64 桌面应用；
2. Linux x64 桌面应用，作为次级支持平台；
3. 本地用户登录、角色和权限；
4. 串口扫描、连接、断开、重连和连接配置；
5. 多设备并行管理；
6. 现有 `AA/BB + func + len + data + checksum` 协议兼容；
7. 电机控制、压力传感器校准、两段弯曲控制；
8. 紧急停止、归中、主动控制、闭环控制和循环寿命检测；
9. 实时状态卡片、数据表格、曲线和单点观察；
10. 三维连续体机器人模型显示与姿态联动；
11. 实验会话录制、历史加载、回放与 CSV 导出；
12. 运行日志、通信日志、原始报文、操作审计；
13. 模拟器、原始报文终端和故障注入；
14. 配置、主题、布局和数据路径管理；
15. 旧版数据迁移；
16. 安装包、版本信息、诊断包和测试交付。

### 4.2 本期不做（Out of Scope）

除非后续单独立项，本期不包含：

1. BLE 蓝牙设备接入；
2. 云账号、云数据库或远程协作；
3. ROS2 作为运行必需依赖；
4. 移动端应用；
5. 机械视觉、YOLO、MediaPipe 或手势识别；
6. 在线算法训练；
7. 远程控制机器人；
8. 未经安全评审的互联网自动更新；
9. macOS 正式适配；
10. 对硬件固件协议进行未经确认的破坏性修改。

---

## 5. 用户角色

| 角色 | 主要职责 | 权限 |
|---|---|---|
| 普通操作员 | 连接设备、查看数据、执行授权范围内的控制、保存实验数据 | 常规设备控制、曲线、历史、会话 |
| 管理员 | 系统配置、日志审计、调试、用户管理和高级控制 | 普通操作员全部权限，以及日志管理、模拟器、原始终端、用户管理、诊断 |
| 维护/调试人员 | 协议联调、故障复现和模拟器测试 | 由管理员授予调试权限，可与管理员为同一账号 |

权限判断必须在 Rust 后端再次校验，不得仅依赖前端隐藏按钮。

---

## 6. 术语

- **设备（Device）：** 通过一个串口连接的连续体机器人控制器。
- **连接（Connection）：** 串口从打开到关闭的一次生命周期。
- **帧（Frame）：** 符合设备通信协议的一组完整字节。
- **设备快照（Device Snapshot）：** 某一时刻的电机、传感器、弯曲角和系统状态集合。
- **会话（Session）：** 一次实验或采集过程，包括数据、配置、日志和备注。
- **连接配置（Connection Profile）：** 端口选择规则、波特率、数据位等配置。
- **控制配置（Control Profile）：** PID、阈值、限幅、超时和重发参数。
- **模型配置（Model Profile）：** 三维模型资源、节点绑定和姿态映射。
- **旧版协议（Legacy Protocol V1）：** 当前 Python 版本使用的 `AA/BB` 帧协议。
- **模拟器（Simulator）：** 不依赖真实串口设备的虚拟数据和命令响应源。

---

## 7. 总体业务流程

```text
启动应用
  → 登录
  → 进入工作区
  → 自动扫描或手动选择串口
  → 建立设备连接
  → 发送握手/唤醒
  → 接收并解析设备状态
  → 更新状态卡片、曲线和三维模型
  → 用户下发控制命令
  → 设备执行并反馈
  → 闭环策略根据反馈继续控制
  → 数据、日志和审计记录进入会话
  → 保存、导出或回放
```

---

# 8. 功能需求

## FR-001 软件启动与本地认证

### 需求说明

软件启动后应先进入本地认证流程，认证成功后进入主工作区。

### 需求细项

1. 支持账号登录；
2. 支持注册普通用户；
3. 支持修改当前用户密码；
4. 支持退出登录；
5. 支持管理员和普通用户角色；
6. 支持“记住用户名”；
7. “记住登录”不得以 Base64 或明文保存密码；
8. 首次安装时应引导创建管理员，或要求修改初始管理员密码；
9. 连续登录失败应有短时限流；
10. 用户管理和密码变更写入审计日志。

### 验收标准

- 正确账号可以登录，错误密码不可登录；
- 普通用户无法调用管理员命令；
- 重启后可恢复用户名；
- 本地数据文件中不存在明文密码或可直接还原的密码；
- 管理员可创建、禁用或重置普通用户。

**优先级：P0**

---

## FR-002 主工作区与导航

### 需求说明

系统应提供统一主工作区，并支持设备、分析、会话、3D、日志和设置等页面。

### 需求细项

1. 提供左侧导航、顶部工具栏、中央工作区和底部状态栏；
2. 支持 Dashboard、设备工作区、曲线分析、三维模型、会话与历史、日志与审计、设置；
3. 支持设备标签页或等效的多设备切换机制；
4. 支持保存并恢复窗口大小、面板比例和最近页面；
5. 页面切换不得影响串口采集和控制循环；
6. 危险操作必须有明确视觉区分；
7. 支持浅色与深色主题。

### 验收标准

- 重启后恢复最近布局；
- 两台设备连接时可快速切换；
- 页面切换期间数据采集连续；
- 紧急停止始终可从设备工作区快速访问。

**优先级：P0**

---

## FR-003 串口发现与连接配置

### 需求说明

系统必须支持扫描、筛选、配置和连接本地串口设备。

### 需求细项

1. 自动扫描可用串口；
2. 显示端口名、描述、制造商、VID/PID、序列号等可用信息；
3. 支持过滤蓝牙虚拟串口、调制解调器和明显无关端口；
4. 支持手动输入端口路径；
5. 支持配置波特率、数据位、校验位、停止位和流控；
6. 默认兼容旧版参数：9600 波特率；
7. 支持保存多个连接配置；
8. 支持连接、断开和手动重连；
9. 支持可配置的自动重连策略；
10. 端口被占用、权限不足、设备拔出时显示可操作的错误提示；
11. 连接和断开写入日志和审计。

### 验收标准

- 能发现并连接真实 USB 串口设备；
- 端口被拔出后在可接受时间内进入断线状态；
- 设备恢复后可手动或自动重连；
- Linux 下可识别 `/dev/ttyUSB*`、`/dev/ttyACM*`、CH341 等常见设备；
- Windows 下可识别 `COM*` 设备。

**优先级：P0**

---

## FR-004 多设备管理与生命周期

### 需求说明

每个串口连接应对应独立设备实例，设备间状态、缓存和控制互不干扰。

### 需求细项

1. 支持同时连接多个设备；
2. 每个设备拥有独立连接状态、接收缓冲、协议解析器、设备状态、控制状态机、PID、曲线缓存和会话记录；
3. 关闭设备时必须停止串口任务、控制任务、定时任务和数据分发；
4. 同一串口不得被重复连接；
5. 设备可设置显示名称；
6. 支持设备状态摘要；
7. 异常设备不得拖垮其他设备。

### 验收标准

- 至少可同时运行两个真实或模拟设备；
- 关闭一个设备后，其后台任务和文件句柄被释放；
- 一个设备断线时，其他设备继续正常工作；
- 重复连接同一端口时给出明确提示。

**优先级：P0**

---

## FR-005 握手与设备识别

### 需求说明

连接成功后，系统应执行握手或唤醒流程，并确定设备是否可用。

### 需求细项

1. 兼容旧版 `0xFE` 握手/唤醒命令；
2. 支持配置握手延迟、超时和重试次数；
3. 握手成功后进入 Ready 状态；
4. 无响应时进入 Degraded 或 Error 状态；
5. 若设备协议支持，应读取设备类型、固件版本和能力；
6. 不支持能力查询的旧设备应使用兼容配置；
7. 握手原始帧写入通信日志。

### 验收标准

- 旧设备连接后可发送兼容握手帧；
- 握手超时不会阻塞界面；
- 无响应时用户可重试或进入受限调试模式。

**优先级：P0**

---

## FR-006 实时数据接收、拼帧与校验

### 需求说明

系统必须持续接收字节流，并在完成帧头、长度和校验和验证后进入业务解析。

### 需求细项

1. 使用增量字节流解析，不假设一次串口读取恰好对应一帧；
2. 支持粘包、拆包和噪声字节恢复；
3. 兼容旧版上行 `BB 02 ...` 状态帧；
4. 校验帧头、功能码、长度、电机/传感器数量、数据区边界和校验和；
5. 校验失败时丢弃最小必要数据并重新寻找帧头；
6. 统计接收字节数、有效帧数、校验失败数和丢弃字节数；
7. 支持原始十六进制查看；
8. 原始日志应可按设备开启或关闭；
9. 解析异常不得导致应用崩溃。

### 验收标准

- 使用拆包、粘包、错帧和随机噪声测试数据时解析器可恢复；
- 无效帧不进入控制和显示流程；
- 错误统计可在诊断页面查看；
- 协议解析有固定测试向量。

**优先级：P0**

---

## FR-007 设备状态模型与滤波

### 需求说明

系统应将状态帧转换为统一设备快照，并对指定字段执行可配置滤波。

### 需求细项

设备快照至少包含：设备 ID、连接 ID、时间戳、电机数量和状态、传感器数量和三通道值、两段目标角和当前角、方向、系统使能状态、协议质量信息和接收序号。

滤波最低支持：

- 3 点中值滤波；
- 单步变化限幅；
- 一阶指数平滑；
- 关闭滤波；
- 原始值和滤波值同时保留。

### 验收标准

- 同一帧的原始值和处理值可追溯；
- 滤波参数修改后对新数据生效；
- 异常大跳变可被限幅或标记；
- 控制算法明确选择使用原始值或处理值。

**优先级：P0**

---

## FR-008 控制系统使能与安全状态

### 需求说明

用户应能启动、关闭和紧急停止设备控制系统。

### 需求细项

1. `0x01`：使能/启动控制系统；
2. `0x00`：失能/关闭控制系统；
3. `0x02`：紧急停止；
4. 紧急停止命令具有最高发送优先级；
5. 紧急停止后清空普通待发送控制命令；
6. 断线、协议异常或设备错误时自动禁止危险控制；
7. 控制状态必须由设备反馈确认，不能只依赖前端按钮；
8. 危险动作前应检查连接、握手、权限和设备状态；
9. 所有安全相关操作写入审计日志；
10. 提供“仅监测”模式。

### 验收标准

- 紧急停止可在连接正常时立即排入最高优先级队列；
- 断线后控制按钮自动禁用；
- 设备未使能时不可发送普通运动命令；
- UI 状态与设备反馈不一致时显示告警。

**优先级：P0**

---

## FR-009 电机控制

### 需求说明

系统应支持单电机位置、速度和加速度控制。

### 需求细项

1. 选择电机 ID；
2. 设置目标位置、速度和加速度；
3. 支持正向和反向；
4. 兼容旧版 `0x03` 电机控制命令；
5. 旧版数值按 ×100 编码并使用大端有符号字段；
6. 参数范围由设备能力或配置文件限制；
7. 发送前显示最终工程值；
8. 发送后记录原始命令帧；
9. 支持命令确认、超时和有限次重发；
10. 支持一键归中，兼容 `0x04`；
11. 可查看目标值和反馈值差异。

### 验收标准

- 可向指定电机发送合法位置、速度、加速度；
- 超范围参数被前后端共同拒绝；
- 归中命令按设备电机数量正确组帧；
- 发送记录可在操作历史中查询。

**优先级：P0**

---

## FR-010 压力传感器校准

### 需求说明

系统应支持按传感器 ID 下发校准值。

### 需求细项

1. 选择传感器 ID；
2. 输入校准值；
3. 兼容旧版 `0x03` 传感器校准命令；
4. 校准值按 ×100 和大端有符号字段编码；
5. 支持零点校准快捷操作；
6. 显示校准前后数据；
7. 校准操作需要权限确认；
8. 校准值和命令帧写入审计日志；
9. 支持保存校准记录到会话。

### 验收标准

- 可向指定传感器发送校准值；
- 错误 ID 或超范围校准值被拒绝；
- 校准记录可追溯。

**优先级：P1**

---

## FR-011 两段式弯曲控制

### 需求说明

系统应支持第一段和第二段连续体的角度、方向及联动控制。

### 需求细项

1. 第一段目标角度范围默认 0–90°，可配置；
2. 第二段目标角度范围默认 0–70°，可配置；
3. 每段支持上、右、下、左四个方向；
4. 支持分别设置两段参数；
5. 支持组合发送；
6. 兼容旧版 `0x05` 两段联动弯曲命令；
7. 显示每段目标角、当前角和误差；
8. 控制前检查设备使能状态；
9. 支持保存常用姿态预设；
10. 三维模型同步显示目标姿态和反馈姿态。

### 验收标准

- 两段角度与方向能正确编码并发送；
- 参数超过配置范围时不可发送；
- 目标和反馈角度在 UI、曲线和 3D 中一致；
- 组合命令被记录。

**优先级：P0**

---

## FR-012 主动控制

### 需求说明

系统应兼容旧版主动控制保持功能。

### 需求细项

1. 兼容 `0x06` 主动控制命令；
2. 支持启用和关闭；
3. 持续发送周期必须可配置；
4. 断线、失能、紧急停止或离开允许状态时自动停止；
5. 防止重复定时任务；
6. 显示当前主动控制状态；
7. 状态变化写入日志和审计。

### 验收标准

- 主动控制按配置周期发送；
- 关闭后不再发送；
- 设备断线时任务自动终止；
- 不会因重复点击创建多个并行发送循环。

**优先级：P1**

---

## FR-013 闭环控制与循环寿命检测

### 需求说明

系统应提供可配置闭环控制，并兼容现有压力阈值、电机反馈和 PID 相关逻辑。

### 需求细项

1. 闭环控制运行在 Rust 后端；
2. 支持 Kp、Ki、Kd、控制周期、积分限幅、输出限幅和死区；
3. 支持压力传感器阈值触发；
4. 支持电机位置到位判断；
5. 支持命令超时和有限次重发；
6. 支持收缩/伸出循环；
7. 显示并保存循环次数；
8. 支持暂停、继续和停止；
9. 断线、紧急停止、状态异常时自动终止；
10. 控制配置可保存为 Profile；
11. 每次状态转换和命令发送写入会话控制轨迹；
12. 支持仅模拟模式运行。

### 验收标准

- 使用模拟器可完成完整收缩/伸出循环；
- 循环次数准确；
- PID 输出受限幅约束；
- 超时重发次数不超过配置；
- 紧急停止后闭环任务停止；
- 相同输入序列得到可重复的控制输出。

**优先级：P0**

---

## FR-014 实时状态与数据表格

### 需求说明

系统应实时展示设备、电机、传感器和控制状态。

### 需求细项

1. 设备总览卡片；
2. 电机状态卡片；
3. 传感器状态卡片；
4. 弯曲角状态；
5. 连接质量和帧统计；
6. 实时表格支持列显示/隐藏、排序、单位、暂停自动滚动和最近 N 行；
7. 高亮异常值、离线状态和越界状态；
8. UI 刷新频率与采样频率解耦；
9. 页面不可见时降低刷新频率；
10. 用户可选择显示原始值或滤波值。

### 验收标准

- 高频数据接收时 UI 可操作；
- 暂停表格不影响后台记录；
- 状态值与最新设备快照一致；
- 长时间运行时前端内存不因表格无限增长。

**优先级：P0**

---

## FR-015 实时曲线与历史分析

### 需求说明

系统应提供电机、传感器和弯曲角实时及历史曲线。

### 需求细项

1. 电机位置、速度、加速度曲线；
2. 传感器三通道曲线；
3. 第一段和第二段目标角/当前角曲线；
4. 支持多个通道叠加或分窗；
5. 支持开始、暂停、恢复和清空显示；
6. 支持缩放、平移、十字游标和单点查看；
7. 支持自动量程和固定量程；
8. 支持时间窗口配置；
9. 支持加载历史会话；
10. 支持截图；
11. 支持曲线数据 CSV 导出；
12. 大数据历史显示需降采样，不修改原始数据；
13. 图表刷新率与数据采样率解耦。

### 验收标准

- 至少三类曲线可同时稳定刷新；
- 历史数据加载不阻塞 UI；
- 游标显示的数值与源数据一致；
- 导出 CSV 可被 Excel 和 Python 正常读取。

**优先级：P0**

---

## FR-016 三维连续体机器人模型

### 需求说明

系统应使用 Three.js 在应用内显示连续体机器人三维模型，并与设备状态联动。

### 需求细项

1. 支持加载本地 glTF/GLB 模型；
2. 支持第一段和第二段模型节点或骨骼绑定；
3. 根据两段方向和角度更新姿态；
4. 支持显示目标姿态和反馈姿态；
5. 支持旋转、缩放、平移和重置视角；
6. 支持网格、坐标轴、骨骼/节点调试显示；
7. 支持模型背景、光照和材质设置；
8. 支持模型配置文件；
9. 模型缺失或节点绑定错误时给出明确诊断；
10. 3D 刷新率与设备采样率解耦；
11. 回放模式下可同步重现姿态；
12. 旧版 `LQTS.html` 仅作为迁移参考，不作为新版本运行依赖。

### 验收标准

- 本地模型可离线加载；
- 实时角度变化可驱动模型；
- 3D 页面关闭或隐藏后释放非必要渲染资源；
- 模型加载失败不会影响设备控制；
- 回放时模型姿态与曲线时间点一致。

**优先级：P1**

---

## FR-017 会话录制与历史数据

### 需求说明

系统应以会话为单位保存实验数据、控制记录和配置快照。

### 需求细项

1. 开始、暂停、继续和停止录制；
2. 自动生成会话 ID；
3. 会话包含元数据、原始帧或原始字节、解析后快照、控制命令、连接配置、控制配置、模型配置、日志摘要、操作员、备注和标签；
4. 支持按日期、设备、操作员和标签检索；
5. 支持会话详情；
6. 支持删除、重命名和归档；
7. 支持导出 CSV；
8. 支持导出和导入会话包；
9. 支持自动保存策略；
10. 写入失败时不得破坏已保存数据。

### 验收标准

- 一次录制可完整停止并再次打开；
- 会话中的数据、配置和控制记录可追溯；
- 导出字段顺序稳定；
- 异常退出后能识别未正常关闭的会话并尝试恢复。

**优先级：P0**

---

## FR-018 会话回放

### 需求说明

系统应使用与实时模式相同的数据分发链路回放历史会话。

### 需求细项

1. 支持播放、暂停、停止；
2. 支持拖动时间轴；
3. 支持 0.25x、0.5x、1x、2x、4x 倍速；
4. 支持逐帧前进；
5. 同步驱动状态卡片、表格、曲线和 3D；
6. 回放模式不得向真实设备发送控制命令；
7. 清晰显示“回放模式”；
8. 支持跳转到错误、命令或阈值触发点；
9. 支持导出选定时间段。

### 验收标准

- 同一会话重复回放结果一致；
- 时间轴跳转后各视图同步；
- 回放期间真实设备控制被禁止；
- 10 万条记录以内可流畅浏览。

**优先级：P1**

---

## FR-019 日志、通信记录与审计

### 需求说明

系统应完整记录运行、通信、控制和用户操作。

### 需求细项

1. 日志等级支持 TRACE、DEBUG、INFO、WARN、ERROR；
2. 分类包括应用、认证、串口、协议、原始收发帧、控制、数据记录、3D、前端错误、崩溃和操作审计；
3. 日志写入本地文件；
4. 按日期和大小滚动；
5. 可配置保留天数和总容量；
6. 支持按时间、等级、模块、设备和关键字筛选；
7. 支持复制和导出；
8. 原始十六进制日志可单独开关；
9. 审计记录不可由普通用户清空；
10. 日志写入不得显著阻塞控制循环；
11. 支持生成诊断包。

### 验收标准

- 连接失败、校验失败、控制超时和导出失败均有日志；
- 可按设备筛选；
- 日志容量受限；
- 诊断包包含版本、配置摘要、日志和最近会话摘要。

**优先级：P0**

---

## FR-020 模拟器与调试模式

### 需求说明

系统必须支持无硬件开发、演示和自动化测试。

### 模拟器模式

1. 正常状态帧；
2. 正弦、三角、阶跃数据；
3. 两段弯曲姿态脚本；
4. 电机目标跟随；
5. 压力阈值循环；
6. 录制文件回放；
7. 多设备模拟。

### 故障注入

1. 丢包；
2. 拆包；
3. 粘包；
4. 校验和错误；
5. 随机噪声；
6. 超量程；
7. 长时间无响应；
8. 物理断线；
9. 命令不确认；
10. 高延迟。

### 调试工具

1. 原始报文终端；
2. 手动发送十六进制帧；
3. 帧解析结果查看；
4. 协议统计；
5. 控制状态机查看；
6. 仅管理员或调试角色可使用。

### 验收标准

- 无硬件时可完成连接、曲线、控制、会话、日志和 3D 演示；
- 模拟故障不会导致程序崩溃；
- 自动化测试可调用确定性模拟器；
- 调试命令写入审计。

**优先级：P0**

---

## FR-021 设置、主题与配置管理

### 需求说明

系统应集中管理应用、连接、控制、模型、主题和存储配置。

### 需求细项

1. 应用设置；
2. 串口连接 Profile；
3. 控制 Profile；
4. 滤波 Profile；
5. 三维模型 Profile；
6. 主题设置；
7. 工作区布局；
8. 日志保留策略；
9. 数据目录；
10. 自动保存策略；
11. 配置导入、导出和恢复默认；
12. 配置文件有版本号；
13. 启动时自动迁移旧版本配置；
14. 配置损坏时回退到安全默认值；
15. 修改危险控制参数时提示影响。

### 验收标准

- 配置重启后保持；
- 无效配置不会导致程序无法启动；
- 配置导入前可预览和校验；
- 旧版本配置可迁移或给出明确处理说明。

**优先级：P1**

---

## FR-022 旧版数据迁移

### 需求说明

系统应提供从 PyQt5 版本迁移数据的能力。

### 迁移来源

1. `~/.lqts/auth_data/users.json`；
2. `~/.lqts/auth_data/config.json`；
3. `~/.lqts/logs/`；
4. `~/experiment_data/data/assets/analyze_data/motor_data`；
5. `~/experiment_data/data/assets/analyze_data/sensor_data`；
6. `~/experiment_data/data/assets/analyze_data/bend_data`；
7. `~/.lqts/auth_data/LQTS.html` 及相关本地资源。

### 需求细项

1. 启动时检测旧版数据；
2. 提供迁移向导；
3. 迁移前显示来源、目标和预计数量；
4. 迁移过程不删除原文件；
5. 用户密码不得直接沿用弱哈希作为最终存储；
6. 旧用户首次登录后要求升级密码；
7. 历史 CSV 可转换为新会话或作为只读历史文件索引；
8. 生成迁移报告；
9. 允许跳过迁移。

### 验收标准

- 迁移不会修改旧目录；
- 可导入旧 CSV；
- 失败项有明确原因；
- 迁移完成后可查看报告；
- 弱密码存储被升级。

**优先级：P1**

---

## FR-023 诊断与版本信息

### 需求说明

系统应提供便于现场联调和问题反馈的诊断能力。

### 需求细项

1. 显示应用版本、构建号、操作系统和架构；
2. 显示 Rust 后端、前端资源和协议版本；
3. 显示串口设备信息；
4. 显示最近错误；
5. 一键导出诊断包；
6. 诊断包默认脱敏用户名、路径和密码；
7. 支持复制系统摘要；
8. 支持检查本地安装包版本；
9. 自动联网更新不属于本期强制功能。

### 验收标准

- 可在无网络环境生成诊断包；
- 诊断包不包含明文密码；
- 版本信息与安装包一致。

**优先级：P1**

---

# 9. 数据需求

## 9.1 标准设备快照

```ts
interface DeviceSnapshot {
  deviceId: string;
  connectionId: string;
  receivedAtMs: number;
  sequence: number;
  protocolVersion: string;
  systemEnabled: boolean;
  motors: MotorState[];
  sensors: SensorState[];
  bend: {
    section1: BendState;
    section2: BendState;
  };
  quality: FrameQuality;
}
```

## 9.2 电机状态

```ts
interface MotorState {
  id: number;
  positionMm: number;
  velocityMmPerSec: number;
  accelerationMmPerSec2: number;
  running: boolean;
  targetPositionMm?: number;
  targetVelocityMmPerSec?: number;
  targetAccelerationMmPerSec2?: number;
}
```

## 9.3 传感器状态

传感器字段在最终协议确认前使用中性命名，同时保留显示别名：

```ts
interface SensorState {
  id: number;
  raw: [number, number, number];
  filtered: [number, number, number];
  aliases?: [string, string, string];
  unit?: string;
  quality: "ok" | "warning" | "invalid";
}
```

## 9.4 会话元数据

至少包含：

- session_id；
- name；
- start_time；
- end_time；
- operator；
- device_ids；
- connection_profiles；
- control_profiles；
- filter_profiles；
- model_profile；
- software_version；
- protocol_version；
- notes；
- tags；
- completion_state；
- record_count；
- checksum/完整性信息。

## 9.5 CSV 导出

默认支持：

- UTF-8；
- UTF-8 BOM；
- 可配置小数位；
- ISO 8601 时间；
- 毫秒时间戳；
- 原始值；
- 滤波值；
- 目标值；
- 反馈值；
- 控制命令；
- 质量标志。

CSV 列名和单位必须在导出文件或配套元数据中明确。

---

# 10. 非功能需求

## NFR-001 离线与隐私

1. 核心功能完全离线可用；
2. 默认不向外网发送任何传感器、日志或账号数据；
3. 不包含强制云服务；
4. 用户明确选择导出位置；
5. 诊断包默认脱敏。

## NFR-002 性能

在参考硬件（4 核 CPU、8 GB 内存、集成显卡）上：

1. 单设备 100 Hz 状态流可连续运行；
2. 两个模拟设备同时运行时界面保持可操作；
3. 控制命令从用户操作到进入发送队列的本地延迟目标小于 50 ms；
4. 状态卡片刷新 10–30 Hz；
5. 曲线刷新 15–30 FPS；
6. 3D 刷新目标 30 FPS；
7. 前端不保存无限历史；
8. 一小时稳定运行内存增长应趋于稳定；
9. 10 万条历史记录可在降采样后浏览。

## NFR-003 可靠性

1. 串口断线、错帧、数据异常不得导致应用崩溃；
2. 写盘失败不得阻塞紧急停止；
3. 紧急停止不应排在普通命令后等待；
4. 未正常结束的会话可识别和恢复；
5. 配置损坏时可使用安全默认值启动；
6. 一个设备故障不得影响其他设备。

## NFR-004 安全性

1. 密码使用抗暴力破解哈希；
2. 记住登录使用操作系统安全凭据或等效安全存储；
3. Tauri 权限按窗口和能力最小化配置；
4. 前端不可直接任意访问文件系统或执行命令；
5. 危险命令在 Rust 后端验证角色、状态和参数；
6. 不加载远程网页作为 3D 资源；
7. 本地模型和配置导入需校验路径和格式。

## NFR-005 可维护性

1. 领域逻辑不得直接写在 React 组件中；
2. 协议解析、控制算法和存储可独立测试；
3. Rust 与 TypeScript 使用类型化契约；
4. 配置文件带 schema/version；
5. 错误代码稳定；
6. 公共接口有文档；
7. 关键架构决策记录为 ADR。

## NFR-006 可测试性

1. 协议解析有 golden fixtures；
2. 控制算法有确定性测试；
3. 串口层可替换为模拟传输；
4. 文件系统可使用临时目录测试；
5. 前端可使用 mock IPC；
6. 关键用户流程有 E2E 测试；
7. 发布构建前自动执行测试。

## NFR-007 可用性

1. 常用设备连接流程不超过三步；
2. 错误提示说明原因和处理建议；
3. 危险操作颜色、图标和文字明确；
4. 数值显示单位一致；
5. 键盘操作可用；
6. 长任务显示进度并允许安全取消；
7. 中文为默认语言，字符串集中管理以便国际化。

## NFR-008 兼容性

1. Windows 10/11 x64 为首要支持；
2. Linux x64 为次级支持；
3. 保持旧版串口协议兼容；
4. 支持旧版 CSV 导入；
5. 安装包不要求用户预装 Python、Node.js 或 Rust；
6. 3D 模型使用本地 glTF/GLB。

## NFR-009 日志与可诊断性

1. 所有跨层错误具有错误码；
2. 每个连接和会话具有关联 ID；
3. 控制命令可从 UI 操作追踪到原始帧；
4. 日志滚动和容量限制可配置；
5. 崩溃后保留最近日志。

---

# 11. UI 需求概述

## 11.1 登录页

- 品牌和版本；
- 用户名、密码；
- 记住用户名；
- 登录、注册、修改密码入口；
- 错误提示；
- 初始管理员安全提示。

## 11.2 Dashboard

- 当前设备数；
- 连接状态；
- 当前会话；
- 采样率/有效帧率；
- 最近错误；
- 快速连接；
- 快速进入最近会话。

## 11.3 设备工作区

- 设备标签；
- 连接状态；
- 使能/失能；
- 紧急停止；
- 两段弯曲；
- 电机控制；
- 传感器校准；
- 闭环和循环检测；
- 当前状态卡片；
- 原始数据摘要。

## 11.4 曲线分析页

- 通道树；
- 图表工作区；
- 实时/历史模式；
- 时间窗口；
- 游标；
- 导出；
- 布局保存。

## 11.5 三维模型页

- Three.js 视口；
- 目标/反馈切换；
- 模型配置；
- 视角控制；
- 节点调试；
- 当前角度；
- 渲染状态。

## 11.6 会话页

- 会话列表；
- 搜索和筛选；
- 录制状态；
- 元数据；
- 回放时间轴；
- 导入/导出；
- 备注和标签。

## 11.7 日志页

- 等级筛选；
- 模块筛选；
- 设备筛选；
- 时间范围；
- 关键字；
- 原始帧；
- 审计；
- 诊断包。

## 11.8 设置页

- 应用；
- 连接；
- 控制；
- 滤波；
- 数据路径；
- 日志；
- 主题；
- 3D 模型；
- 用户管理；
- 旧版迁移；
- 关于。

---

# 12. 整体验收标准

项目正式验收至少满足：

1. 可在全新 Windows 10/11 x64 机器安装并启动；
2. 无需预装 Python、Node.js 或 Rust；
3. 可完成本地登录和角色校验；
4. 可扫描并连接真实串口；
5. 可兼容现有握手和主要控制命令；
6. 可稳定接收和解析状态帧；
7. 可显示电机、传感器和两段角度；
8. 可完成使能、紧急停止、电机控制、归中和弯曲控制；
9. 可通过模拟器完成闭环循环；
10. 可显示实时曲线；
11. 可录制、停止、打开和导出会话；
12. 可查看和导出日志；
13. 可使用 Three.js 加载并联动本地模型；
14. 可导入至少一组旧版 CSV；
15. 断线、错帧和写盘错误不会使程序崩溃；
16. 自动化测试通过；
17. 安装包、源代码、文档和测试报告齐全。

---

# 13. 需求追踪与优先级

| 优先级 | 含义 |
|---|---|
| P0 | 首个正式版本必须完成，否则不可替代旧版 |
| P1 | 正式版本应完成，可在明确风险接受后延后 |
| P2 | 增强功能，可进入后续版本 |

首轮开发建议优先顺序：

1. 串口、协议和模拟器；
2. 设备状态和安全命令；
3. 电机、传感器和弯曲控制；
4. 闭环控制；
5. 实时 UI 和曲线；
6. 会话、日志和导出；
7. 认证和权限；
8. Three.js 3D；
9. 旧版迁移；
10. 打包和发布。

---

# 14. 迁移策略要求

1. 旧版 PyQt5 应在新版本功能对齐前保持可用；
2. 先冻结协议测试向量，再重写解析器；
3. 新旧软件对同一输入帧的解析结果必须对比；
4. 新旧软件对同一控制参数生成的命令帧必须对比；
5. 真实设备联调采用分阶段白名单；
6. 第一个正式版本不得在无回退方案的情况下替换现场软件；
7. 发布前完成至少一次并行运行对比。

---

# 15. 里程碑建议

## M0：基线冻结

- 收集真实协议帧；
- 固化命令和状态帧测试向量；
- 确认传感器字段单位；
- 确认闭环触发规则；
- 建立旧版功能清单。

## M1：基础框架

- Tauri 2 工程；
- React 工作区；
- Rust 日志、配置和错误模型；
- CI；
- 模拟器骨架。

## M2：连接与协议

- 串口扫描；
- 多连接；
- Legacy V1 协议；
- 握手；
- 状态快照；
- 原始日志。

## M3：控制闭环

- 安全状态机；
- 电机、校准、归中、弯曲；
- PID；
- 循环寿命；
- 超时重发。

## M4：可视化

- 设备工作区；
- 实时表格；
- 曲线；
- Three.js 模型；
- 主题和布局。

## M5：数据与审计

- 会话；
- 回放；
- CSV；
- 日志和诊断；
- 用户和权限；
- 旧数据迁移。

## M6：测试与发布

- 真机联调；
- 长稳测试；
- 安装包；
- 用户手册；
- 发布说明；
- 并行运行和最终切换。

---

# 16. 最终交付物

1. Windows 安装包；
2. Linux 安装包或可复现构建说明；
3. 源代码仓库；
4. Rust/TypeScript API 文档；
5. Legacy V1 协议说明；
6. 配置 schema 和样例；
7. 模拟器及故障脚本；
8. 自动化测试；
9. 测试报告；
10. 用户使用手册；
11. 本 SRS；
12. 配套 TDS；
13. 迁移报告模板；
14. 发布说明；
15. 已知问题列表；
16. 诊断包格式说明。

---

# 17. 待确认事项

以下内容在编码冻结前必须由项目负责人或硬件负责人确认：

1. 上行状态帧的完整字节偏移和长度定义；
2. 三个传感器字段的实际物理含义和单位；
3. 电机、角度、传感器数值比例；
4. 每个命令的设备确认机制；
5. 闭环压力高低阈值；
6. 电机收缩/伸出目标位置；
7. 命令超时和最大重发次数；
8. 第一段、第二段机械安全角度范围；
9. 紧急停止后恢复流程；
10. 3D 模型节点或骨骼命名；
11. 正式支持的最大设备数量；
12. Windows 和 Linux 的最终支持级别。

未确认项应通过配置或兼容适配层隔离，不得散落为硬编码。

---

# 18. 完成定义（Definition of Done）

一个版本只有在同时满足以下条件时才可标记完成：

1. 对应需求有实现和测试；
2. 协议与控制逻辑通过 golden test；
3. 关键错误有日志和用户提示；
4. 无高优先级已知安全问题；
5. 无阻断级崩溃；
6. 安装包在干净环境验证；
7. 模拟器全链路通过；
8. 真机关键流程通过；
9. 数据导出经过校验；
10. 文档与版本号同步；
11. 迁移和回退方案可执行；
12. 产品负责人和硬件负责人完成验收。
