# SoftUI 更新日志

## 2026-07-23

### 修复

- 修复切换主题后认证会话被空快照覆盖，导致需要重新登录的问题。
- 修复设置页账户管理、诊断导出和旧版迁移面板在窗口缩放时重叠、遮挡和显示不全的问题。
- 修复工作区、监控页、表格页、控制页、会话页、设置页的多处自适应布局问题。
- 修复电机控制卡片中当前位置与目标位置显示风格不一致的问题。
- 修复深色模式下串口选择下拉列表文字颜色过浅、不可读的问题。
- 移除回放状态条的全局显示，避免非必要条带占用界面空间。
- 优化左侧导航栏，支持向左收起，并在窄窗口下自动进入图标导航状态。

### 优化

- 统一页面网格、面板、按钮、输入框和表格的最小尺寸约束，降低窗口缩放导致的文字裁剪和控件溢出。
- 优化监控页串口发现区域布局，减少大面积空白，并让端口卡片按可用宽度自动换列。
- 优化控制页的系统状态、电机控制、传感器校准和弯曲控制区域高度分配，避免底部面板覆盖主控制区域。
- 优化设置页布局，将应用配置、账户权限、诊断操作和旧版迁移固定到清晰的区域，避免纵向堆叠互相挤占。

### 后端

- 后端 `set_theme`、`update_settings`、`toggle_connection` 现在返回包含当前认证态的完整运行快照。

### 说明

- 本次更新不包含 3D 展示开发。
- 推送版本过滤了本地运行状态、日志、构建产物、依赖目录和可能包含隐私信息的配置文件。
