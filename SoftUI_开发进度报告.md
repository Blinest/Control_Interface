# SoftUI 开发进度报告

生成日期：2026-07-15  
项目目录：`D:\LuckyStar\Desktop\研究生工作\2025-2026研一下\绳驱连续体机器人\SoftUI`  
当前开发目标：SoftUI 上位机迁移项目  
当前暂缓内容：3D 展示、Three.js 模型联动、3D 独立窗口

## 1. 项目定位

SoftUI 当前应按两份项目文档推进：

- `SoftUI_SRS_需求规格说明书.md`
- `SoftUI_TDS_开发技术设计说明书.md`

项目目标是迁移原 Python/PyQt 上位机能力，形成基于 Tauri 2、Rust、React、TypeScript 的桌面上位机软件。按照 TDS 架构要求，Rust 后端负责串口通信、Legacy Protocol V1、设备运行时、安全控制、日志、存储和诊断；React 前端只负责界面展示和用户交互。

当前明确不做 3D 展示。SRS/TDS 中与 3D 相关的页面、模型、节点绑定、Three.js 渲染和 3D 刷新率目标，暂不计入近期开发范围。

## 2. 当前总体进度

当前项目处于“P0 上位机核心闭环建设期”。后端协议和控制骨架已经明显推进，前端控制页已经开始接入真实 Tauri 命令，但距离完整上位机交付仍有较大差距。

总体判断：

- 后端协议编码与基本命令：基本完成。
- 后端串口运行时：初步完成，仍缺后台长期任务和重连机制。
- 安全控制队列：基础完成，急停高优先级逻辑已实现。
- 前端控制页：已接入后端命令，并新增可调参数表单，仍需布局和文案清理。
- 实时数据、曲线、会话、日志、诊断、设置、迁移：仍处于原型或未完成阶段。
- 3D：按当前要求暂停。

最近验证状态：

- `cargo test`：27 个 Rust 测试通过。
- `npm run build`：通过。
- 最新运行实例来自 `softui-desktop`，端口使用 `1421`。

## 3. 当前已完成内容

### 3.1 工程与运行环境

已完成：

- 建立 `softui-desktop` Tauri 2 工程。
- 前端使用 React + TypeScript。
- 后端使用 Rust + Tauri command。
- Vite dev 端口固定为 `1421`，用于避免与相邻项目冲突。
- 当前工作重心已纠正为 SoftUI 上位机项目，不再混入 `ThreeSoft`。

仍需完善：

- 没有形成正式打包验收流程。
- 还没有完整 Windows 安装包、版本发布、干净机器验证流程。
- 还没有 CI 或自动化端到端测试流程。

### 3.2 Legacy Protocol V1

已完成：

- 实现状态帧解析。
- 实现流式拼帧和校验。
- 实现坏校验后的重同步测试。
- 实现以下命令编码：
  - `0x00`：失能 / 关闭控制系统。
  - `0x01`：使能 / 启动控制系统。
  - `0x02`：紧急停止。
  - `0x03`：电机控制。
  - `0x03`：传感器校准。
  - `0x04`：一键归中。
  - `0x05`：两段联动弯曲。
  - `0x06`：主动控制保持 tick。
- 已有命令帧黄金测试，覆盖电机、传感器校准、弯曲、主动控制、系统控制等帧。

仍需完善：

- 需要从原 Python 项目和真实设备采集更多协议 fixture。
- 需要补充真实异常帧、丢包、粘包、空读、半帧、连续多帧测试。
- 需要增加协议统计信息，例如总帧数、校验失败数、丢帧数、解析错误类型。

### 3.3 串口与设备运行时

已完成：

- `SerialTransport`：真实串口读写封装。
- `SimulatorTransport`：无硬件调试模拟器。
- `DeviceRuntime`：设备运行时，支持握手、轮询状态、发送命令、关闭。
- `DeviceRegistry`：设备注册表，支持连接、断开、列出设备、轮询所有设备。
- `poll_all()`：可轮询所有已连接串口设备。
- 真实串口帧可以覆盖 RuntimeSnapshot，使前端优先显示真实设备状态。

仍需完善：

- 当前轮询仍由前端 `tick_snapshot` 间接驱动，不是真正独立的后台采集任务。
- 缺少长期运行任务模型。
- 缺少断线检测、自动重连、重连退避、关闭清理的完整状态机。
- 缺少多设备并发运行的稳定性验证。
- 缺少串口配置表单，包括波特率、数据位、校验位、停止位、流控。

### 3.4 安全控制与命令队列

已完成：

- 增加 `CommandPriority`。
- `DeviceRuntime` 增加待发送命令队列。
- 普通命令按队列发送。
- 急停命令使用 Emergency 优先级。
- 急停会清空普通待发送命令，并优先发送。
- `RuntimeStatus` 增加 `pending_commands`。
- 已有测试确认急停会清空普通待发送命令。

仍需完善：

- 需要把队列从“立即 flush”推进到真实后台发送队列。
- 需要在 UI 上显示待发送队列状态。
- 需要将“设备未使能时禁止普通运动命令”做成统一后端 guard。
- 需要急停后的恢复流程。
- 需要急停、失能、运动命令的完整审计链。

### 3.5 Tauri 后端命令

已完成：

- `submit_system_control`
- `send_motor_command`
- `send_home_command`
- `calibrate_sensor`
- `send_bend_command`
- `send_active_control_tick`
- `list_serial_ports`
- `connect_device`
- `disconnect_device`
- `list_connected_devices`
- `tick_snapshot`
- `bootstrap_state`
- `export_diagnostics_bundle`

仍需完善：

- 前端还没有完整接入真实串口连接配置和连接流程。
- 诊断导出内容仍偏基础。
- 控制命令参数校验仍需集中放在 Rust 后端。
- 命令执行结果缺少明确的业务状态返回，例如发送成功、设备拒绝、未使能、急停中、串口断开。

### 3.6 前端页面

已完成：

- Dashboard 总览页面原型。
- 设备工作区页面原型。
- 表格页原型。
- 控制页原型。
- 曲线页原型。
- 日志页原型。
- 设置页原型。
- 3D 入口当前已按要求暂停或绕开。

控制页当前新增：

- 电机命令表单：
  - 电机通道。
  - 目标位置 mm。
  - 速度。
  - 加速度。
- 传感器校准表单：
  - 传感器编号。
  - 校准值。
- 弯曲命令表单：
  - 第一段方向。
  - 第一段角度。
  - 第二段方向。
  - 第二段角度。
- 控制按钮：
  - 使能 / 失能。
  - 紧急停止。
  - 一键归中。
  - 主动控制。
  - 电机发送。
  - 传感器校准。
  - 发送弯曲。

仍需完善：

- 前端仍存在历史编码错误导致的乱码文案。
- 当前控制页布局仍偏原型，需要做上位机工作流级别的整理。
- 串口连接页和控制页之间缺少完整联动。
- 表格、曲线仍需绑定真实后端快照缓冲区。
- 仍需确保所有窗口尺寸下无滚动条、无窗格裁切、无按钮飞出。

## 4. SRS/TDS 需求覆盖矩阵

| 模块 | 文档要求 | 当前状态 | 说明 |
|---|---|---|---|
| FR-003 串口发现与连接配置 | 串口扫描、连接配置、连接管理 | 部分完成 | 已有扫描和基础连接命令，配置项不完整 |
| FR-004 多设备管理与生命周期 | 多设备连接、关闭清理、生命周期 | 部分完成 | 有 DeviceRegistry，但后台任务和关闭清理仍需加强 |
| FR-005 握手与设备识别 | 握手、设备识别、错误处理 | 部分完成 | connect 时可握手/探测，但识别信息和重试不足 |
| FR-006 实时数据接收、拼帧与校验 | 持续接收、拼帧、校验、异常处理 | 部分完成 | 协议解析较完整，后台持续接收未完成 |
| FR-008 使能与安全状态 | 使能、失能、急停、优先级 | 部分完成 | 命令和急停优先级已实现，恢复流程和 guard 待补 |
| FR-009 电机控制 | 兼容 `0x03`，电机参数控制，一键归中 | 部分完成 | 后端命令和前端表单已有，真实设备闭环待测 |
| FR-010 压力传感器校准 | 兼容 `0x03` 传感器校准 | 部分完成 | 后端命令和前端表单已有，校准流程待完善 |
| FR-011 两段式弯曲控制 | 兼容 `0x05`，方向和角度控制 | 部分完成 | 后端命令和前端表单已有，反馈联动待完善 |
| FR-012 主动控制 | 兼容 `0x06` 主动控制 | 部分完成 | 单次 tick 已有，持续主动控制任务未完成 |
| FR-014 实时状态与数据表格 | 实时表格、设备快照 | 部分完成 | UI 原型已有，真实 ring buffer 待实现 |
| FR-015 实时曲线与历史分析 | 实时曲线、历史曲线 | 原型阶段 | 曲线仍偏模拟数据，需要接真实缓冲区 |
| FR-019 日志、通信记录与审计 | 分类日志、收发帧、审计 | 部分完成 | 控制命令有审计日志，系统性日志仍不足 |
| FR-021 设置、主题与配置管理 | 设置持久化、主题、路径 | 部分完成 | 主题/基础设置已有，配置 schema 不完整 |
| FR-022 旧版数据迁移 | 旧 Python 数据、配置、日志迁移 | 未完成 | 尚未开展 |
| FR-023 诊断与版本信息 | 诊断包、版本、环境信息 | 部分完成 | 有基础导出，内容和流程待完善 |
| Phase 6 / 3D | Three.js、本地 GLB、模型联动 | 暂停 | 按当前要求暂不做 |

## 5. 当前风险

### 5.1 技术风险

- 前端 `App.tsx` 仍然过大，功能集中，后续维护成本高。
- 当前实时数据仍没有独立后台采集任务，长期运行稳定性不足。
- 命令队列目前仍是同步 flush，尚未形成真正异步队列。
- 串口断线和重连机制仍不足。
- 控制参数校验分散，后端统一安全 guard 不完整。

### 5.2 质量风险

- 前端存在乱码文案，影响使用和验收。
- 部分页面仍有原型展示和模拟状态。
- UI 曾出现窗格裁切、按钮飞出、滚动条等问题，需要持续回归检查。
- 尚未建立 Playwright 或 Tauri E2E 测试。

### 5.3 项目管理风险

- SRS/TDS 覆盖面较大，当前仍在核心闭环阶段，不能过早进入视觉优化或 3D。
- 如再次混入 `ThreeSoft` 或其它相邻项目，会导致开发轨道偏离。
- 后续每次改动都必须继续验证端口和进程属于 SoftUI。

## 6. 下一阶段开发计划

### 阶段 A：前端文案与控制页可用性修复

目标：让设备工作区达到可读、可用、可验收的上位机控制界面。

任务：

1. 清理 `App.tsx` 中乱码文案。
2. 梳理导航、按钮、面板标题、状态字段。
3. 精修控制页布局，保证任意窗口尺寸下不裁切、不飞出、不出现非预期滚动条。
4. 将电机控制、传感器校准、弯曲控制、主动控制分区明确。
5. 将当前状态、连接状态、使能状态、急停状态清晰展示。

### 阶段 B：真实串口生命周期

目标：从前端 tick 驱动转为 Rust 后台任务驱动。

任务：

1. 建立设备采集 worker。
2. 建立设备命令 worker。
3. 明确状态机：
   - Idle
   - Connecting
   - Handshaking
   - Ready
   - Error
   - Closed
4. 支持断线检测。
5. 支持自动重连和手动重连。
6. 关闭设备时停止串口任务、控制任务和数据分发。

### 阶段 C：实时数据缓存

目标：让表格和曲线使用真实设备数据。

任务：

1. Rust 侧建立 ring buffer。
2. 缓存电机状态、传感器状态、弯曲角度、协议质量。
3. 前端表格从真实缓存读取。
4. 曲线从真实缓存读取。
5. 统计帧率、采样率、丢帧、校验错误。

### 阶段 D：安全控制完善

目标：满足 SRS/TDS 对安全控制的硬要求。

任务：

1. 普通运动命令必须检查设备使能状态。
2. 急停后禁止普通运动命令，直到执行恢复流程。
3. 急停后停止主动控制和闭环控制任务。
4. 命令队列改为后台发送队列。
5. UI 显示队列状态和最近控制结果。

### 阶段 E：日志、审计、诊断

目标：形成可诊断上位机。

任务：

1. 记录原始收发帧。
2. 记录协议错误。
3. 记录串口错误。
4. 记录控制审计。
5. 完善诊断包导出：
   - app 信息。
   - runtime snapshot。
   - 串口信息。
   - 协议统计。
   - 最近日志。
   - 设置文件。

### 阶段 F：会话与导出

目标：满足实验数据记录和回放需求。

任务：

1. 建立 SQLite 或等价本地存储。
2. 支持会话开始、暂停、继续、停止。
3. 支持 CSV 导出。
4. 支持历史回放。
5. 支持异常会话恢复。

### 阶段 G：设置、权限、迁移

目标：补齐交付所需基础能力。

任务：

1. 完善设置 schema。
2. 完成路径配置。
3. 完成布局配置。
4. 实现登录、角色权限。
5. 迁移旧 Python 项目的配置、日志、历史数据。

## 7. 暂不开发内容

以下内容暂不进入近期开发：

- 3D 窗口。
- Three.js 模型加载。
- GLB/gltf 节点绑定。
- 3D 相机、HUD、骨骼调试。
- 3D 与曲线/设备状态联动。

暂停原因：当前核心上位机闭环尚未完成。优先级应放在串口、协议、安全控制、实时数据、日志、会话和可诊断性上。

## 8. 当前建议的立即下一步

建议下一步执行“阶段 A：前端文案与控制页可用性修复”。

理由：

- 当前后端控制命令骨架已经具备。
- 前端控制页已能调用后端，但文案乱码严重。
- 乱码和布局问题会直接影响使用判断，也会让后续调试真实设备时难以定位问题。
- 修复文案和控制布局不会改变协议风险，是当前收益最高的整理工作。

建议任务顺序：

1. 修复导航、页面标题、控制页、表格页、日志页乱码。
2. 保留当前已接入的命令按钮和表单。
3. 精简控制页面板，避免窗格裁切。
4. 再进入后台串口任务和实时数据缓存开发。

## 9. 开发执行规则

后续所有开发继续遵守以下规则：

1. 只在 SoftUI 项目内开发。
2. 不使用、不启动、不复制 `ThreeSoft`，除非用户明确要求归档或复制。
3. 3D 展示暂不做。
4. 每次修改完成后：
   - 关闭旧 SoftUI 实例。
   - 清理 SoftUI 自己的 `1421` 服务。
   - 打开最新版本。
   - 确认运行进程来自 `softui-desktop`。
5. 修改代码后至少执行：
   - `npm run build`
   - `cargo test`
6. UI 修改后需要额外关注：
   - 无非预期滚动条。
   - 无窗格裁切。
   - 无按钮飞出。
   - 无无意义条状数据展示。
   - 非必要窗格不跨页面重复出现。

